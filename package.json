{
  "name": "lockertrack",
  "version": "1.0.0",
  "description": "Gestionnaire de colis en consignes et points relais",
  "scripts": {
    "build": "echo 'No build step'",
    "sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
